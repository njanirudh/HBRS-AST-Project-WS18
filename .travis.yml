  language: python
  python:
    - "3.6"
  install:
    - pip install pytest
    - pip install pytest-cov
    - pip install coveralls
  script:
    - cd ast_project_2018
    - python3 test_cases.py --cov
  after_success:
  - coveralls
